package model;
/**
 * observer interface for Observer pattern
 */
public interface Observer {
	/**
	 * update method for Observer pattern
	 * @param i , input to differentiate different cases
	 */
	void update(int i);
}
